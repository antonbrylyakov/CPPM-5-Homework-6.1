#pragma once

// для простоты берем операции над целыми числами
namespace IntegerArithmetic
{
	int sum(int x, int y);

	int substract(int x, int y);

	int multiply(int x, int y);

	float divide(int x, int y);

	float power(int x, int y);
}